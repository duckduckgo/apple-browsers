# Project Log

## Current handoff
- Goal: Validate and, only when explicitly requested, publish the completed iteration-one iOS Promo Queue Q3 slice without putting temporary project documentation on the app branch.
- Status: Local `bartosz/promo-q-3` is implemented at `29a7e33a80` directly from frozen `bartosz/promo-q-2-fixes` tip `37b99b0d78`. Its four dependency-ordered commits cover removal animation, pure cooldown/history, admission/checkpoints, and read-only diagnostics. The branch is not pushed and no pull request has been opened.
- Completed: Branch guard and fetch, implementation, focused coverage, strict static validation, forbidden-scope audit, and durable documentation updates. The app branch contains no project documentation, telemetry, privacy-config rollout, live flag transitions, plural owners, provisional reservations, boundary timers, or optional atomic unique-shown work.
- Next: Execute the focused simulator suites and manual QA for the standard NTP, suggestion tray, and unified-input hosts when the simulator harness is available. Push or open a PR only after a separate explicit request.
- Blockers: The simulator harness stalled during animation validation, and the later local approval service rejected the final rerun before launch. Runtime and manual visual proof remain pending; static validation is green.

## Decisions
### 2026-07-28 — Land through three serial app PRs
- Decision: Use `bartosz/promo-q-1`, `bartosz/promo-q-2`, and `bartosz/promo-q-3` as a serial stack; keep the now-renumbered Step 11 privacy-config rollout separate.
- Why: This follows the implementation plan's dependency slices while avoiding parallel-stack rebases and repeated propagation of review feedback.
- Consequences: `bartosz/promo-queue` is only the temporary source snapshot. Normally review feedback flows through merged `main`; because PR 2 was final but not yet merged when PR 3 preparation began, the user explicitly chose to resynchronize the source with final `bartosz/promo-q-2` and base PR 3 directly on it. Nothing under `promo-queue-docs/`, and no `project_log.md`, may enter the app PRs.

- Treat the implementation on `bartosz/promo-q-2-fixes` and the agreed Q2/Q3 simplification plans as authoritative over the pre-simplification `TECH_DESIGN_FINAL.md`; keep the consolidated tech design current with those decisions.
- Use one app-scoped, main-actor transactional lease arbiter constructed in `Launching` and passed explicitly; do not add another global singleton or extract the macOS queue.
- Preserve provider order, eligibility, modal-to-modal cooldown, and accounting; on the feature-enabled path acquire the modal lease before any provider is queried because provider evaluation can have side effects. The later fixed modal/RMF directional policy is service-owned. Feature off retains the lease-free legacy overload and existing modal cooldown.
- Model NTP activity explicitly in all three hosts. Cached focused NTPs can remain mounted and on-window while hidden, so UIKit appearance alone is not authoritative.
- Treat atomic `remoteMessageShownUnique` coordination as an optional independent correctness change requiring separate approval, not a Q3 cooldown prerequisite.
- Default the new iOS promo-queue feature flag to disabled, sample it once per service graph, and stage enablement in a separate privacy-configuration change. Remote/local changes affect a fresh graph/relaunch, not the running process.
- Extend the existing Modal Prompt Coordination debug screen. Do not add Promo Queue admission, collision, or cooldown telemetry in iteration one; measurement belongs to a separate project.

### 2026-08-11 — Adopt the Q2 simplification endpoint and re-plan Q3
- Decision: Treat `bartosz/promo-q-2-fixes` at `37b99b0d78` as the agreed Q2 endpoint: one immutable process mode, one singular identity-bearing owner, one release-only self-owned RMF admission, truthful physical removal, one guarded weak handoff registry, and the modal/NTP lifecycle adapters.
- Flag consequence: Delete live transition semantics from the final design. A kill-switch or local-override change applies only to a new graph; production rollback therefore requires force-quit/relaunch.
- Ownership consequence: Two NTP RMFs cannot coexist. The 10-minute RMF→RMF rule is a minimum delay after confirmed appearance, but physical ownership can defer the next RMF longer. This is stricter than the original per-surface design and increases hidden-host starvation risk.
- Retry consequence: Q3 is checkpoint-only. Do not add the old exact-boundary RMF timer; reaching 10 minutes or 24 hours alone does nothing.
- Q3 scope: Implement the fixed directional policy and confirmed-RMF persistence/appearance confirmation against the singular owner, restore the coordinated scale/opacity removal animation without early release, add current-architecture diagnostics, and add only the focused cooldown/animation coverage missing from Q2-fixes.
- Old Q3 disposition: `origin/bartosz/promo-q-3` at `59dfec29f7` is pre-simplification evidence. Do not port its live state, plural owners, provisional reservation, boundary scheduler, production in-memory fallbacks, or stale debug rows.
- Deferred scope: Atomic unique-shown accounting remains optional; telemetry and privacy-config rollout remain separate projects.

### 2026-08-06 — Add fixed directional modal/RMF cooldowns to iteration one (product matrix retained; implementation shape superseded)
- Decision: Add modal→RMF 10 minutes, RMF→RMF 10 minutes globally across IDs/NTP instances, RMF→modal fixed 24 hours, and preserve the existing remotely tunable modal→modal interval (currently/default 24 hours).
- Source events: Confirm modal history only at UIKit completion or attached-root adoption, and RMF history only on the first `onAppear` of the matching admitted render session. Selection, mapping, admission alone, denial, pending/failed modal work, and withdrawal before appearance do not write.
- State ownership: Keep `PromoQueueLeaseArbiter` transient/history-free. A separate service-owned policy/store reuses the existing persisted modal timestamp and adds one persisted last-confirmed-RMF timestamp. Store event times, not expiry dates; exact boundaries are eligible, and backward wall-clock movement conservatively extends cooldown.
- Concurrency/retry: Take one global identity-bound provisional RMF reservation before publication, promote it on matching appearance, and release it without writing on withdrawal. Schedule at most one active-NTP retry at the earliest 10-minute boundary; modal retry remains foreground/checkpoint-driven. A later RMF may appear after 10 minutes even while an earlier RMF remains visible, but active RMF leases still block modals.
- Flag/accounting: Feature off bypasses new checks, RMF writes, provisional state, and timers while preserving confirmed timestamps and the legacy modal cooldown. Cooldown denial retains work and consumes no provider/RMF accounting. Iteration one emits no new Promo Queue telemetry.
- Scope: This is compiled iteration-one policy for launch modals and NTP RMF only, not a generic RMF frequency engine or remotely configurable queue. PR 3 [#6217](https://github.com/duckduckgo/apple-browsers/pull/6217) implements it with focused boundary/persistence/concurrency coverage in `06a2417373`.
- Supersession note: The four product rows remain approved. The provisional reservation, per-surface coexistence, live flag behavior, and exact-boundary timer described in this entry were replaced by the 2026-08-11 singular-owner/checkpoint-only decision. The old implementation is test evidence, not the final Q3 base.

### 2026-08-06 — Defer Promo Queue telemetry
- Decision: Keep the iteration-one debug snapshot, but remove the two newly introduced collision pixels and all related reporter/definition/test code from PR 3 (`1f12bf8a66`).
- Why: Promo Queue telemetry will be designed and delivered as a separate project rather than expanding this implementation's scope.
- Consequences: Iteration one relies on debug state plus existing RMF/provider accounting. No Promo Queue admission/collision/cooldown pixel is a release or definition-of-done requirement.

## Recent progress
### 2026-07-23
- Initialized project memory for the promo queue implementation-planning work.
- Validated the lease design against the current iOS implementation with three parallel read-only audits.
- Identified factual drift that the implementation plan must handle: seven current modal providers, three NTP construction paths, retained/hidden embedded NTPs, up to three eager/appearance shown-accounting calls, missing iOS flag mapping/config, and uncancellable delayed modal presentation.
- Wrote and independently reviewed `promo-queue-docs/IMPLEMENTATION_PLAN.md`, including concrete runtime ownership, file changes, dependency-ordered work, behavioral tests, QA, debug state, the then-proposed pixel scope, and staged rollout. The pixel scope was later removed.
- Began implementation coordination for Steps 1–9; Step 10 remains an external rollout action.
- Completed Step 1 in commit `b63dc0f30e` with an explicit disabled default and existing local-override support.
- Completed Step 2 in commit `44bf021796`; static Swift parsing and Xcode project validation passed, while tests remain unrun pending permission.
- Completed Step 3 in commit `ba02fd4988`; all changed Swift parsed, the app module reached Swift emission, and the build later stopped in the existing `Copy GRDB framework` script.
- Completed Step 4 in commit `b5a43051d6`; review caught and corrected two Xcode 26.4 compile errors, after which the app passed Swift compilation for both simulator architectures and stopped only at the environment-dependent signing identity script.
- Completed Step 5 in commit `d0724ca115`; independent review caught late-completion, background-verification, and route-re-adoption gaps, which were fixed with durable receipt state and focused coverage. The app again passed Swift compilation before the environment-dependent signing identity script stopped the overall build.
- Completed Step 6 in commit `e5e316a04f`; two independent review passes caught stale same-ID gate callbacks, non-animated tray activation, premature transition-barrier release, transition-time registration gaps, and UTI attachment timing. The corrected production sources compiled successfully, with the overall build stopping only at the existing GRDB identity script; tests remain unrun pending permission.
- Completed Step 7 in commit `117d8c72c3`; independent review confirmed shared main-actor first-shown reservations, dual-NTP and live-disable race coverage, persisted-state behavior, and a concurrency-safe delayed-store test seam. Production Swift compilation succeeded before the known signing identity script failure; tests remain unrun pending permission.
- Completed Step 8 in commits `4e07b2833f` and `d3307ea8ef`; multiple review passes caught stale tray/opacity completions, a duplicated protocol seam, after-idle expectation drift, and missing real service-to-model live-toggle coverage. The final matrix covers real manager/service/arbiter flows, all NTP owner paths, atomic accounting races, lifecycle fallbacks, transition reentrancy, expiry, and exact-root history. Static validation passed and the full iOS Browser Debug production build succeeded; tests remain unrun pending permission.
- Implemented and committed the Step 9 debug snapshot and extended Modal Prompt Coordination debug screen in `1219a11694`, threading the same read-only provider through both construction paths; Swift parsing passed and production sources compiled before the known GRDB copy-script failure.
- Added and independently reviewed `promo-queue-docs/ADDING_PROMOS.md` in `4d1bb4b8df`; all 17 relative links resolve and the guide distinguishes exact-root reconciliation from provider evaluation when modal admission is blocked.
- Audited the two then-proposed Step 9 aggregate pixels without editing definitions. That proposal was later superseded by the decision to move all Promo Queue telemetry to a separate project.
- Completed a requirement-by-requirement read-only audit of Steps 1–9. No production defect was found; the audit identified missing focused flag-contract and coordinated modal-policy coverage, repaired in `6e4fa8d6d0` and `110496bc2a`. All changed Swift parses, the Xcode project validates, and the full diff passes whitespace checks; test execution remains unverified pending permission.
- Reached the safe in-project implementation boundary. The then-remaining telemetry contract was measurement-owner-controlled; it was later removed from iteration one entirely.

### 2026-07-27
- Rebased the branch onto `main` (`25e68c2706`), replaying all 37 commits with subjects, order, and authorship preserved. Every commit hash cited in this log was rewritten and has been updated in place; the pre-rebase tip is retained as tag `backup-promo-queue-prerebase`.
- Only one of the six files touched by both sides conflicted: `UnifiedInputContentContainerViewController.swift`, where `main` and this branch each added an `else` branch to `setActive(_:)`. Resolved by keeping both statements — `fireSearchSuggestionsDisplayPixels()` then `setPromoSurfaceHostActive(false)`. Verified the branch's net changes are otherwise byte-identical pre- and post-rebase across all six files.
- `project.pbxproj` auto-merged; all four promo-queue Swift files keep their four registration entries each, and all 20 iOS Swift files `main` added are registered. `plutil -lint` passes.
- Confirmed `main`'s new `tabDidRequestAIChatFeedback` sheet needs no promo-queue wiring: `ModalPromptCoordinationManager.swift:560` already blocks admission on any non-dismissing `presentedViewController`, not just promo-owned ones.
- Full `iOS Browser` scheme build succeeded with zero errors after the rebase — the first clean end-to-end app build in this project, since earlier steps stopped in the environment-dependent GRDB/signing script phases. Correct scheme name is `iOS Browser`; `iOS Browser Debug` does not exist and `xcodebuild` exits 0 on that error.
- Correction to the earlier entries: the test targets did not merely go unrun, they did not **compile**. `build-for-testing` on both `iOS Unit Tests` and `iOS Integration Tests` failed at `MockFeatureFlagger.swift:35` with `missing return in getter`. Commit `ba02fd4988` added `updatesPublisherAccessCount += 1` to the `updatesPublisher` getter, turning a single-expression body into two statements so Swift's implicit return no longer applied. Pre-existing on this branch and unrelated to the rebase; every prior step's "tests remain unrun" note was therefore hiding a hard compile break. Fixed by adding the explicit `return`.
- Compile the test targets with `build-for-testing` after any change to a shared mock in `iOS/SharedTestUtils/`. The app scheme does not build those files, so app-only builds cannot catch this class of error.
- Three further latent compile errors in this branch's own test code, all also predating the rebase and all invisible to an app-only build: `#expect(callbackAdmissions.allSatisfy(\.isUnavailableDuringTransition))` (key path into a `rethrows` method expands to a throwing call inside the macro — replaced with an explicit closure); `ModalPromptCoordinationManagerTests` referencing `MockSwitchBarHandler`, which is `private` to two other test files (added a local private copy, matching how those two files each declare their own); and `NewTabPageMessagesModelTests.createSUT` constructing the `@MainActor` `MockNewTabPagePromoCoordinator` in a default-argument expression (moved into the function body).
- Both test schemes now reach `** TEST BUILD SUCCEEDED **` and SwiftLint is clean on all four changed files. Note the verdict banner differs by action: `build` prints `** BUILD ... **`, `build-for-testing` prints `** TEST BUILD ... **`.
- Checked the Smartling branch `origin/smartling-deletions-u9f9r1jn3y0b-20260727-142056` for translations belonging to this feature: there are none. This branch changes no `.strings`/`.xcstrings`/`.lproj` file and adds no `NSLocalizedString`/`UserText` call site — the promo queue only coordinates promos whose copy is already localized, and the new debug-menu labels are hardcoded English per the convention in other `*Debug*.swift` screens. All 41 keys on that Smartling branch belong to AI Chat, VPN, and Settings work.
- `main` advanced two commits after the rebase, to `1cd28f85e6`: a macOS Prompt Bar icon change and a broad unused-asset removal. The branch remains intentionally two commits behind while this scoped implementation is finalized; catching up is separate follow-up work because the asset-removal commit also edits the iOS project file.
- Committed the four latent test-compile fixes in `256defb822` and the corresponding log update in `66341efa46`. Pre-rebase tip remains at tag `backup-promo-queue-prerebase`.
- Finalized a Step 9 analytics contract with owner `bkunat`; this historical contract was subsequently superseded when telemetry moved out of iteration-one scope.
- Implemented and independently reviewed the telemetry in the temporary source history. PR 3 later removed the complete feature in `1f12bf8a66`, including events, reporter, definitions, and tests.
- Static Swift parsing, project `plutil`, diff checks, and focused Prettier validation pass. A concrete-simulator `iOS Browser` build succeeds with consistent ad-hoc signing. `build-for-testing` succeeds for both `iOS Unit Tests` and `iOS Integration Tests`; no tests were executed.
- `npm run validate-pixel-defs` historically validated the two proposed definitions before failing on unrelated unchanged schemas. Those Promo Queue definitions were later removed from PR 3 in `1f12bf8a66`.
- Initialized the repository's pinned `privacy-reference-tests` submodule at `c2b49fe7` after the first runtime attempt exposed missing test resources. The submodule checkout does not change the parent worktree.
- Corrected two promo-queue test issues found only by the canonical scheme: the focused-NTP attachment test now suppresses automatic UIKit appearance forwarding while testing real window attachment (`2fba4a66df`), and `NewTabPageControllerDaxDialogTests` is main-actor isolated to match its UIKit setup and coordinator dependency (`a0f851d6c7`).
- Use the CI-configured `iOS Browser` scheme for the authoritative iOS test gate. The standalone `iOS Unit Tests` scheme omits the `testing` launch argument and repository skip list, producing false failures in unrelated full-app observers, Simulator-gated scheduling, and a known CDN-dependent integration test.
- The canonical CI-equivalent command (`iOS Browser`, iPhone 17 Pro / iOS 26.4 Simulator, three attempts with retry-on-failure) passed: 7,509 tests passed, 70 skipped, zero failed. Result bundle: `Test-iOS Browser-2026.07.27_19-48-05-+0200.xcresult`.

### 2026-07-28
- Audited every promo-queue document for PR, stacked-branch, and cherry-pick language; none exists. `IMPLEMENTATION_PLAN.md:519-531` says not to combine the feature into one change and maps Steps 1–10 into three dependency-ordered app slices.
- Confirmed the likely remembered “four changes” are three app slices plus the separately required privacy-configuration rollout (`IMPLEMENTATION_PLAN.md:829-853,986-999`), not four documented app PRs.
- Compared the completed implementation history with current `main`. The existing step commits are suitable cherry-pick units; only `FeatureFlag.swift`, pixel/debug/project files, and several UTI files overlap newer `main` work.
- Identified the minimum correction routing: the `MockFeatureFlagger` return and SharedState actor isolation belong with the Step 3 injection slice; the NTP default-argument and attachment-fixture fixes belong with the NTP slice; the integration key-path and local switch-bar mock fixes belong with behavioral integration coverage.
- Rebased all 44 source commits onto `main` with no conflicts; autostash preserved and restored the two unstaged temporary documentation changes.
- Commit hashes in progress entries before this point refer to the pre-2026-07-28 source history; after the rebase, use commit subjects or `git log main..bartosz/promo-queue` to resolve their current hashes.
- Created local-only `bartosz/promo-q-1` from `main` in a separate worktree and cherry-picked the Step 1–4 implementation plus its early test corrections. The only conflict was the feature-flag test's project-file context; the resolution kept its four intended project entries and excluded later host-visibility entries.
- Opened [#6072](https://github.com/duckduckgo/apple-browsers/pull/6072) as a draft from `bartosz/promo-q-1` to `main` using the repository PR template.

### 2026-08-04
- PR 1 was reviewed and merged as [#6087](https://github.com/duckduckgo/apple-browsers/pull/6087) in `7fdd4719a1`; the earlier draft [#6072](https://github.com/duckduckgo/apple-browsers/pull/6072) is historical rather than the landed PR identifier.
- Verified the landed implementation directly from `origin/main`: `.promoPresentationCoordination` → `iOSPromoQueueSubfeature.iOSPromoPresentationCoordination`, `PromoCoordinationService`/`PromoCoordinationFactory`, typed per-lease acquisition outcomes, identity-safe invalidation/release, and weak-token pruning.
- Reviewed and incorporated the existing implementation-plan/handoff edits and the new `promo-queue-docs/rmf_gap_analysis.md`; temporary docs remain excluded from app PRs.
- Created local safety branch `bartosz/promo-queue-pre-rebase-20260804` at pre-rebase tip `5595ca1337`, then rebased `bartosz/promo-queue` onto updated `main` `7fdd4719a1`. The rebased implementation tip before this handoff update is `3d24ca5bd4`.
- Dropped the original flag, arbiter, injection, modal-phase, flag-contract, modal-policy, and SharedState PR 1 commits/fixes because reviewed main supersedes them. Kept the NTP default-argument and integration key-path portions of the mixed `iOS: Fix unit tests` commit.
- Resolved Step 5–9 conflicts by keeping reviewed names and contracts, moving surviving additions into the reviewed promo-specific service/manager test suites, retaining both reviewed and host-visibility Xcode project entries, and excluding obsolete `ModalPromptCoordinationServiceTests`/legacy-manager test copies. No stale production `ModalPromptCoordinationService`, `.promoQueue` feature flag, or `modalPromptCoordinationService` identifiers remain.
- Verified `main` is an ancestor, the safety branch still points to `5595ca1337`, the worktree was clean before this handoff edit, and `git diff --check` passed. `bartosz/promo-queue` is intentionally not pushed and diverges from `origin/bartosz/promo-queue` by 153 local-only commits versus 44 old remote commits.
- Created `bartosz/promo-q-2` just in time from reviewed `main` and ported Steps 5–6. The final reviewed stack also includes the provider revalidation/replacement contract, all three NTP host visibility/coverage/animation paths, explicit reachable cleanup with weak registration/token pruning, and flag-off regression coverage. Step 7 accounting, the remaining integration coverage, one standalone stale alpha-completion guard, telemetry/debugging, and temporary project documentation remain excluded.
- The focused `iOS Browser` gate covered `PromoCoordinationServicePromoQueueTests`, `ModalPromptCoordinationManagerPromoQueueTests`, `NewTabPageMessagesModelTests`, and `ModalPromptCoordinationManagerIntegrationTests`: 82 selected tests passed as 90 executed cases, with zero failures or skips. Compilation also surfaced and fixed narrow PR 1 compatibility issues in actor-isolated defaults, test helper naming, reviewed lease snapshots, presentation-host fixtures, and exact-root lifecycle expectations.
- Pushed only `bartosz/promo-q-2` and opened draft [#6175](https://github.com/duckduckgo/apple-browsers/pull/6175) against `main`. After review changes, its final tip is `8d6d95438e`; the PR contains no project documentation.

### 2026-08-06
- Created safety branch `bartosz/promo-queue-pre-pr2-sync-20260806` at `ec56ca19b7`, then rebuilt local `bartosz/promo-queue` on the exact final PR 2 tip `8d6d95438e` without modifying or force-pushing `bartosz/promo-q-2`.
- Incorporated PR 2 review changes into the source: the flag now lives in `FeatureFlags-iOS` as `.promoPresentationCoordination` → `iOSPromoQueueSubfeature.iOSPromoPresentationCoordination`; providers revalidate prepared/retained prompts and may safely replace invalid work; the NTP gate uses stable gate/session/mount identities and composed host renderability; dead retry registrations prune even while disabled; and feature-off modal/NTP behavior is pinned as the legacy path.
- Reduced the then-remaining source implementation to five app commits: atomic NTP first-shown accounting (`0e702e9bea`), behavioral integration coverage (`564b687755`), the standalone stale alpha-completion guard (`ae5b14734a`), debug state (`c7ef72fc7a`), and collision telemetry (`cf414d4ddd`). The telemetry slice was later removed from PR 3 in `1f12bf8a66`.
- Created `bartosz/promo-q-3` directly from `8d6d95438e` and replayed those five app changes as `54e1cc4720`, `80f8a38cb2`, `6a6ec011cb`, `46753d95c9`, and `c9c1de13c0`. The branch was subsequently pushed and opened as draft PR [#6217](https://github.com/duckduckgo/apple-browsers/pull/6217); its exact base is final PR 2 and it contains no temporary project documentation.
- Updated every affected Promo Queue Markdown/HTML artifact for the approved directional cooldown matrix, including ownership, persistence, provisional RMF serialization, boundary retry, feature-off behavior, PR 3 scope, tests/QA/debug, and the distinction between RMF framework gaps and the narrow iOS client policy. No app code was changed by this documentation pass.
- Removed the two Promo Queue collision pixels and their implementation/tests from PR 3 in `1f12bf8a66`; telemetry is now explicitly a separate project.
- Implemented the documented directional cooldown policy on PR 3 in `06a2417373`, including the persisted RMF timestamp, global provisional reservation, exact-boundary retry, debug snapshot, and focused service/policy/model/integration coverage. `origin/bartosz/promo-q-3` now points to this commit.

### 2026-08-11
- Fetched and audited `origin/bartosz/promo-q-2` at `12e24676eb` and `origin/bartosz/promo-q-2-fixes` at `37b99b0d78`; their merge-base confirms the fixes branch is a clean stack on the current Q2 tip.
- Verified the agreed fixes in code: one factory flag read and no publisher subscription; one singular arbiter owner; release-only RMF admission; physical removal through outgoing mount state and the next main turn; readiness-gated stable weak handoff; centralized known-host exposure; one provider validity hook; cached retained Default Browser validation; and the preserved named `0.1`-second modal delay.
- Identified the complete core Q3 remainder: directional cooldown policy/store/appearance confirmation; checkpoint-only cooldown integration; read-only current-architecture diagnostics; restoration of the coordinated scale/opacity removal animation; and focused coverage/QA for those changes. No other core product behavior was found missing.
- Flagged accepted concerns relative to the original design: next-process rollback, stronger singular-owner serialization/starvation risk, checkpoint-only delay, manual future-host integration, cached Default Browser staleness, and separately deferred atomic unique-shown accounting.
- Rewrote the canonical technical design, implementation/landing plan, integration guide, and visual appendix; added `promo-queue-docs/Q3_IMPLEMENTATION_PLAN.md`; refreshed iteration research status notes and durable lessons. Those planning updates were committed separately at `633e46ada0` and remain outside the app branches.
- Started from a clean app worktree and honored the existing-branch guard: local `bartosz/promo-q-3` already existed, so it was inspected rather than overwritten. It was exactly at frozen Q2-fixes tip `37b99b0d78` with zero divergence. Fetch confirmed `origin/bartosz/promo-q-2-fixes` still matched and pruned the deleted old remote Q3 ref; the base branch was not modified.
- Restored the coordinated scale/opacity removal in `26f11cee84`. `onDisappear` now begins removal only; an iOS-15-compatible inert `AnimatableModifier` reports the real terminal value, and identity-checked outgoing state retains the owner through the following settling turn without a fixed delay.
- Added the pure directional cooldown policy and persisted confirmed-RMF store in `c5cfe59937`, including exact inclusive boundaries, later-of-modal/RMF logic, backward-clock behavior, reconstruction, and read/write-failure semantics.
- Integrated owner-before-policy modal/RMF admission, exactly-once appearance confirmation, raw cooldown rollback, and checkpoint-only reconsideration in `6d2563d963`. Focused service/model/integration coverage includes no-build/no-account denial, elapsed-time no-op, refresh/foreground checkpoints, physically mounted singular ownership after 10 minutes, and no nested drain.
- Added the read-only debug projection in `29a7e33a80`, preserving singular owner and modal attempt identities, live weak-registration count, readiness, confirmed histories, and derived boundaries. Legacy snapshots do not read cooldown history; both direct and settings debug paths receive the same service provider.
- Final static gates pass: all changed Swift files parse, strict SwiftLint passes with cache disabled, the Xcode project plist validates, and app/docs diffs pass whitespace checks. A focused Xcode run caught and repaired one stale admission initializer, but simulator runtime did not produce a clean final result; animated/disabled transition tests and manual three-host QA remain pending.
- Re-audited the final app history: no merge/WIP/fixup commits, live flag transition machinery, plural owners, provisional reservation, cooldown timer/scheduler, Promo Queue telemetry/pixels, privacy-config rollout, atomic unique-shown change, or temporary documentation is present. No push or PR action was taken.
