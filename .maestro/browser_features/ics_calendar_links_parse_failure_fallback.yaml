# ics_calendar_links_parse_failure_fallback.yaml
# Malformed .ics file should show the parse-failure toast and fall back to QuickLook.
# URL points at the local fixture server during development; swap to
# https://privacy-test-pages.site/features/download/calendar-malformed.ics once the
# eduardo/ics-download-fixtures branch lands on privacy-test-pages.
appId: com.duckduckgo.mobile.ios
tags:
    - iphone
    - release

---

- runFlow:
    file: ../shared/setup.yaml

- assertVisible:
    id: "searchEntry"
- tapOn:
    id: "searchEntry"
- inputText: "http://localhost:8000/calendar-malformed.ics"
- pressKey: Enter

- extendedWaitUntil:
    visible: "This file can't be processed. Try opening the downloaded file instead."
    timeout: 10000
