appId: com.duckduckgo.mobile.ios
tags:
    - iphone
    - release
    - unified_toggle_input
name: test_chat_tab_toolbar_buttons

---

- launchApp:
    appId: "com.duckduckgo.mobile.ios"
    clearState: true
    clearKeychain: true
    arguments:
        isOnboardingCompleted: ${ONBOARDING_COMPLETED}
        currentAppVariant: ${APP_VARIANT}
        isRunningUITests: true
        ff.browsingMenuSheetEnabledByDefault: "true"
        ff.unifiedToggleInput: "true"

# Open Duck.ai chat tab
- tapOn:
    id: "Browser.OmniBar.Button.AIChat"

- runFlow:
    when:
        visible: "Agree and Continue"
    commands:
        - tapOn: "Agree and Continue"
- runFlow:
    when:
        visible: "×"
    commands:
        - tapOn: "×"
- runFlow:
    when:
        visible: "×"
    commands:
        - tapOn: "×"

- assertVisible: "Close tab"

# Verify all toolbar buttons are present on the chat tab
# Note: Tools and Attach use UIMenu (showsMenuAsPrimaryAction); their menu items
# are not accessible to Maestro's a11y bridge — only the buttons themselves are verified here.
- assertVisible:
    id: "AIChat.Toolbar.Button.ModelChip"
- assertVisible:
    id: "AIChat.Toolbar.Button.Reasoning"
- assertVisible: "Tools"
- assertVisible: "Attach"
