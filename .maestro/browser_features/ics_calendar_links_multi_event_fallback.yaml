# ics_calendar_links_multi_event_fallback.yaml
# Multi-VEVENT .ics file should show the "multiple events" toast and fall back to QuickLook.
appId: com.duckduckgo.mobile.ios
tags:
    - iphone
    - release

---

- launchApp:
    appId: "com.duckduckgo.mobile.ios"
    clearState: true
    clearKeychain: true
    arguments:
        isOnboardingCompleted: ${ONBOARDING_COMPLETED}
        currentAppVariant: ${APP_VARIANT}
        isRunningUITests: true
        ff.browsingMenuSheetEnabledByDefault: "true"
        ff.icsCalendarLinks: "true"

- assertVisible:
    id: "searchEntry"
- tapOn:
    id: "searchEntry"
- inputText: "https://privacy-test-pages.site/features/download/calendar-multi.ics"
- pressKey: Enter

- extendedWaitUntil:
    visible: "Multiple events aren't supported. Try opening the downloaded file instead."
    timeout: 10000
